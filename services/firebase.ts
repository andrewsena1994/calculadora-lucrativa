// Firebase integration removed as per user request.
// This file is kept empty to prevent import errors if it's referenced elsewhere, 
// though all references should be removed in storage.ts.

export const isFirebaseConfigured = false;
export const auth = null;
export const db = null;
export const app = null;
